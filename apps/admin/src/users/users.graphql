 type User{
    _id: String
    username: String
    createdAt: String
    updatedAt: String
}

type Query{
    userList:[User]
    findUser(id:String):User
}

type Mutation{   
    addUser(user:inputUser):User
}

input inputUser{   
    username: String
}